import React, { PureComponent } from 'react';

export default class Groups extends PureComponent {
  constructor(props) {
    super(props);
    this.state = {};
  }
  render() {
    return (
      <div>
        Dashboard Groups
      </div>
    );
  }
}
